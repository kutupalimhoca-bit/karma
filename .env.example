# Example env for Fusion Starter
# Persistence disabled by default — system runs in ephemeral mode
ENABLE_PERSISTENCE=0

# MongoDB: e.g. mongodb://user:pass@host:27017 (only used if ENABLE_PERSISTENCE=1)
MONGO_URL=
MONGO_DB_NAME=fusion

# Redis (optional, only used if ENABLE_PERSISTENCE=1)
REDIS_URL=redis://localhost:6379

# JWT secret (set a secure one in production)
JWT_SECRET=change-me-to-a-secure-secret

# TURN credentials (if using coturn)
TURN_URL=turn:your-host:3478
TURN_USER=turnuser
TURN_PASS=turnpass

# Room TTL (ms) after last participant leaves; 0 = immediate deletion
ROOM_TTL_MS=0

# SURGE deploy host (your-project.surge.sh)
SURGE_DOMAIN=

# Client: Signaling server base URL (where Socket.IO server runs), e.g. https://api.example.com
VITE_SIGNALING_URL=
